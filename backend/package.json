{
    "private": true,
    "scripts": {
        "prettier-fix": "prettier --write ."
    },
    "devDependencies": {
        "@prettier/plugin-php": "0.24.0",
        "prettier": "3.6.2",
        "serverless-domain-manager": "^8.0.0"
    }
}
