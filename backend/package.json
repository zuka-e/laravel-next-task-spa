{
    "private": true,
    "scripts": {
        "prettier-fix": "prettier --write ."
    },
    "devDependencies": {
        "@prettier/plugin-php": "^0.18.4",
        "prettier": "2.6.2",
        "serverless-domain-manager": "^5.1.5"
    }
}
